const Loading = () => {
    return (
        <div className={"flex flex-col justify-center place-items-center w-full h-[50vh]"}>
            <p>Loading...</p>
        </div>
    )
}

export default Loading;